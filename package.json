{
  "name": "miniprogram-hold",
  "version": "0.2.1",
  "description": "Hold Page's lifecycle until the 'startPage' method trigger.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mingenesis/miniprogram-hold.git"
  },
  "author": "ming",
  "main": "lib/miniprogram-hold.js",
  "module": "es/miniprogram-hold.js",
  "files": [
    "miniprogram_dist",
    "lib",
    "es",
    "src"
  ],
  "scripts": {
    "prepublish": "npm run clean && npm run build",
    "clean": "rimraf lib es miniprogram_dist",
    "lint": "miniprogram-scripts lint",
    "build": "miniprogram-scripts build"
  },
  "dependencies": {},
  "devDependencies": {
    "miniprogram-scripts": "^0.3.0",
    "rimraf": "^2.6.2"
  }
}
